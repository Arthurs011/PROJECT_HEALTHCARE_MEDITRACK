[project]
name = "meditrack"
version = "2.0.0"
description = "MediTrack v2 — Patient Health Record & Risk Management System"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]